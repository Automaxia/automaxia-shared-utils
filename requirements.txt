# Core dependencies - compativel com Python 3.13.2
requests>=2.32.0
python-decouple>=3.8
tiktoken>=0.7.0
litellm>=1.40.0

# JobRunner (scheduler local + servidor HTTP de webhook). Necessario quando
# o produto usa automaxia_utils.JobRunner. Se o produto nao usar jobs, essas
# libs nao sao carregadas em runtime — import e' lazy em jobs.py.
APScheduler>=3.10.0
croniter>=2.0.0

# Optional: LangChain support (instalar com: pip install automaxia-utils[langchain])
# langchain>=0.3.0
# langchain-community>=0.3.0

# Optional: Provider-specific token counting (instalar com: pip install automaxia-utils[providers])
# anthropic>=0.25.0
# google-generativeai>=0.5.0